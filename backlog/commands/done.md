---
description: Mark a backlog task as done
---

Mark the backlog task matching "$ARGUMENTS" (an id or title fragment) as done, per the backlog skill's conventions. If more than one task matches, list the candidates and ask which one instead of guessing.
