---
description: Add a new task to the backlog
---

Add a new backlog task titled "$ARGUMENTS" following the conventions in the backlog skill (file naming, frontmatter schema, id assignment). If priority or tags aren't obvious from the title, use sane defaults rather than asking. Confirm with the new task's id and file path.
