---
description: List backlog tasks, optionally filtered
---

List backlog tasks per the backlog skill's conventions (grouped by status, sorted by priority within group). If arguments are given ("$ARGUMENTS"), treat them as a filter — a status, priority, or tag — and only show matching tasks.
